extends ../../layouts/default.pug
include ../../mixins/box-head.pug
include ../../mixins/moment.pug

block main
    .container.my-3 
        .row 
            .col-12 
                +box-head(pageTitle)

        .row 
            each song in songs
                .col-6.mb-3  
                    .song-item
                        .inner-image 
                            a(href=`/songs/detail/${song.slug}`)
                                img(src=song.avatar, alt=song.title)
                        .inner-content 
                            .inner-title
                                a(href=`/songs/detail/${song.slug}`) #{song.title}
                            .inner-singer <i class="fa-solid fa-microphone-lines"></i> #{song.infoSinger.fullName} 
                            .inner-like <i class="fa-solid fa-thumbs-up"></i> #{song.like.length}
                            .inner-time <i class="fa-solid fa-clock me-1"></i>
                                +formatDate(song.createdAt)